import React from 'react';

const LoginPage = () => {
    return (
        <div style={{ padding: '50px', maxWidth: '600px', margin: '50px auto' }}>
            <h1>Inicio de Sesión / Registro de Usuario (H001)</h1>
            <p>Aquí construiremos el formulario de Registro (POST) y Login (Fetch API).</p>
        </div>
    );
};
export default LoginPage;